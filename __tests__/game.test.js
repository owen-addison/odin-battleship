import Game from "../src/game";

describe("Game Setup Tests", () => {
  // Test that the game loop requests ship placement from the human player for the required number of ships
  test.skip("Game to request ship placement from the human player for the required ships", () => {});
});
